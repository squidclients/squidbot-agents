# Clean Slate Notes

This repo contains only reviewable source, config templates, sample fixtures, tests, and docs.

It intentionally does not include:

- real customer data
- API keys
- CRM credentials
- sent-message logs
- runtime dashboard exports

The agent is approval-first and sends zero external messages by default.
